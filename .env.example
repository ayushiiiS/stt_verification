SARVAM_API_KEY=your-sarvam-api-key-here
SARVAM_MODEL=saaras:v3
SARVAM_STT_MODE=codemix
SARVAM_REQUEST_INTERVAL_SEC=1
SARVAM_PARALLEL_WORKERS=5
SARVAM_NUM_SPEAKERS=2
SARVAM_FIRST_SPEAKER_ROLE=assistant
# SARVAM_DOWNLOAD_MAX_BYTES=209715200
# SARVAM_DOWNLOAD_RETRIES=3
PORT=5050
FLASK_SECRET_KEY=change-me-to-a-long-random-string
# Optional password overrides: username:password,username2:password2
# Default password for each user is their username
# AUTH_PASSWORDS=ayushi:secret,kriti:secret,akash:secret,yash:secret

# Google Cloud Storage persistence (required for deploy)
# Layout: gs://$GCS_BUCKET/<Agent>/<call_id>/{meta,recording,human,agent,transcripts}
GCS_BUCKET=goldenset1
GCS_LOCATION=asia-south1
# Either set GOOGLE_APPLICATION_CREDENTIALS=/path/to/sa.json
# or paste the full service-account JSON (or base64) here for Vercel:
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
# Set to true to force local-only mode:
# GCS_DISABLED=false
# GCS_AUDIO_WORKERS=4

# Call labeling — Gemma infers domain + subdomain from original transcript
LABEL_PROVIDER=gemma
GEMMA_API_URL=https://your-gemma-proxy/chat/completions
GEMMA_API_KEY=your-gemma-api-key
GEMMA_API_ID=your-inference-deployment-id
# LABEL_PARALLEL_WORKERS=3
# Token savers (defaults shown):
# LABEL_MAX_TRANSCRIPT_CHARS=3000
# LABEL_MAX_TURNS=8
# LABEL_MAX_CHARS_PER_TURN=180
# LABEL_INCLUDE_RATIONALE=false

# Optional: use Google Gemini/Gemma via Generative Language API
# GEMINI_API_KEY=your-google-api-key-here
# LABEL_MODEL=gemma-3-12b-it

# Optional: use OpenAI-compatible API instead
# LABEL_PROVIDER=openai
# LABEL_API_KEY=sk-...
# LABEL_MODEL=gpt-4o-mini
# LABEL_API_BASE=https://api.openai.com/v1

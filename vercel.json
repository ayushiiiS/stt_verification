{
  "buildCommand": "mkdir -p public && rm -rf public/static && cp -R static public/static",
  "rewrites": [
    { "source": "/((?!static/).*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "{templates/**,static/**}"
    }
  }
}
